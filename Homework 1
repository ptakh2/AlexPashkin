#include <stdio.h>
#include <stdlib.h>

void Find_determinant(double** matrix)
{
    for (int i = 0; i < n; ++i) // идем по первому столбцу
    {
        
    }
}

int main()
{
    int n;
    scanf("%d", &n);

    double** matrix = (double**)malloc(n*sizeof(double*));
    for (int i = 0; i < n; ++i)
    {
        matrix[i] = (double*)malloc(n*sizeof(double));
    }

    for (int i = 0; i < n; ++i)
    {
        for (int j = 0; j < n; ++j)
        {
            scanf("%d", &matrix[i][j]);
        }
    }

    Find_determinant(matrix);

    for (int i = 0; i < n; ++i)
    {
        free(matrix[i]);
    }
    free(matrix);
    return 0; 
}
